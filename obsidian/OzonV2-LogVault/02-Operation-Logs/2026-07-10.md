# 2026-07-10 操作日志 (Operation Log)

## 模块化自动化工具台定版 (Modular Automation Workbench Approved)

- 用户确认取消系统自动搜索和判断 1688 同款。
- Ozon 采集完成后，工具台只向用户提供产品名称、图片和尺寸原始证据。
- 用户自行寻找真实供应商并提交 1688 商品链接，浏览器扩展负责采集用户确认的单一 SKU、采购价、起批量、国内运费、属性和图片。
- 采集模块 (Collection)、生图模块 (Image Generation)、上传模块 (Publishing) 必须分离，只通过结构化数据合同交接。
- 自动调度器 (Orchestrator) 仍负责完整流水线；模块分离不能增加“下一步”按钮。
- 工具台改为批次总览、商品采集、图片生成、商品上传、运行日志五个独立视图，日志固定高度滚动。
- 第一版真实发布保留人工确认门禁，草稿确认后才允许 Seller API 提交。
- 不改动旧插件，不共享旧插件运行数据。

设计文档：`docs/superpowers/specs/2026-07-10-modular-automation-workbench-design.md`

当前状态：架构设计已确认并完成自检；用户已批准进入问题盘点和修复阶段。

## 当前问题盘点 (Current Issue Inventory)

- 已建立完整问题清单：`docs/issues/2026-07-10-current-issues.md`。
- Python 全量测试单次运行 85 项通过，但发布门禁用例重复 40 次失败 8 次，确认后台执行器存在竞态。
- `runner.stop()` 目前只设置内存标记，自动循环内部没有逐步取消探针，停止不是硬边界。
- 真实批次 `wb-fe5bee73618b` 记录 Seller 精准类目模板匹配失败，当前文本匹配算法缺少稳定映射与校正回路。
- 自动调度目前只实现到 Ozon 采集门口；1688 用户链接采集、生图和上传尚未接通。
- 浏览器扩展当前只有 Ozon 和本地工具台权限，没有 1688 内容脚本。
- 工具台服务当前未运行，健康检查无法连接；现有启动脚本没有 PID、防重复启动和健康确认。
- `local_server.py` 与 `workbench_service.py` 职责过多，后续必须按已确认的三个模块拆分，但不能与基础故障混修。

第一轮实施计划：`docs/superpowers/plans/2026-07-10-foundation-stability.md`

第一轮只修：后台执行器竞态、停止边界、工具台进程生命周期。通过全部测试后再进入精准类目和 1688 用户链接采集。

## 新批次未自动采集根因 (New Batch Auto-Collection Root Cause)

- 最新批次 `wb-cff6ef01586f` 不是完全没有执行：已完成店铺已有商品去重、抽取种子 `seed-0483`，随后停在俄语查询词门禁。
- `seed-0483` 是“纸质吊牌”，本地关键词生成器没有对应词，返回 `query.needs_generation`。
- 500 个种子包中的俄语查询词字段全部为空；现有本地生成器可覆盖 404 个，剩余 96 个无法生成，阻断比例为 19.2%。
- 浏览器扩展后台轮询被配置为 `allowCreate: false`、`allowNavigate: false`，只有点击扩展图标才允许创建或导航 Ozon 标签页。这是此前为防重复弹窗而引入的反向问题。
- 当前工具台服务也未运行，`127.0.0.1:8765` 没有监听进程；服务重启后内存执行器状态不会自动恢复。

结论：新批次自动采集同时受三个阻断影响：查询词包不完整、扩展自动开页被禁用、工具台进程缺少可靠生命周期。已记录为 OZV2-015、OZV2-016，并排在精准采集之前修复。

## 基础执行链修复 (Foundation Execution Repair)

- 自动调度服务增加停止探针，每次状态迁移前检查用户停止请求。
- 后台执行器把停止信号传入服务层；测试确认停止后线程真实退出，不再被晚到的阻塞状态覆盖。
- 自动执行器运行期间，人工动作接口返回 `runner.busy`，避免同一批次并发修改。
- 发布锁 HTTP 测试与批次自动启动隔离；连续执行 40 次，失败 0 次。
- 新增 `scripts/workbench_control.ps1`，支持启动 (Start)、停止 (Stop)、状态 (Status)、PID 防重复和健康检查。
- 生命周期实测：`STARTED` -> `ALREADY_RUNNING` -> `RUNNING` -> `health.ok` -> `STOPPED`。

## 新批次自动采集入口修复 (New Batch Auto-Collection Entry Repair)

- 新增 96 个种子精确俄语查询映射，补齐原本无法生成查询词的种子。
- 整包 500 个种子逐项通过查询词生成与现有安全校验，缺失数为 0。
- 浏览器扩展升级到 `0.1.6`；工作台发现可执行任务后会自动接单，不再要求用户点击扩展图标。
- 扩展采用“一个任务键对应一个受控标签”的规则：首次任务开一个标签，同一任务重复轮询不再开页或反复跳转，下一采集阶段复用原标签并只导航一次。
- 扩展行为测试和三份 JavaScript 语法检查通过。
- Python 全量测试 89 项通过，耗时 10.843 秒。

当前边界：自动进入 Ozon 采集的入口已经修复；Seller 精准类目映射失败、Ozon 之后的供应商链接采集、生图和上传仍是独立未完成模块，不能标记为端到端完成。

## 新批次真实验证 (New Batch Live Verification)

- 工作台启动后创建真实批次 `wb-b7fcc4a077aa`，目标数量 1。
- 自动完成店铺已有商品去重，店铺商品数量 72。
- 自动抽取 `seed-0046`，生成俄语查询词 `миска`。
- 自动生成 Ozon 属性模板任务合同，状态进入 `attribute_template_collecting`。
- 浏览器任务接口返回 `browser_task.attribute_template_ready`，证明服务端已自动到达浏览器采集入口。
- 发现 Edge 当前心跳仍为旧扩展 `0.1.4`，并保存着已停止的旧批次 ID；旧版因此持续返回 `browser_task.none`。
- 扩展 `0.1.6` 增加旧批次回退：当前批次无可执行任务时查询最新活动任务，并立即更新当前批次 ID。
- 工作区只存在一份 Ozon V2 扩展目录：`browser_extension/ozon_v2_bridge`。浏览器重新加载该目录后，现有真实批次可继续，不需要重新创建。
