# 2026-07-06 操作日志 (Operation Log)

### 1688首页入口门禁 (1688 Homepage Entry Gate) - run-671115c119b7
- 时间 (Time): 2026-07-06
- 问题 (Issue): 1688 以图搜款不能从搜索结果页、详情页、direct `air.1688.com` URL 或其他中间页开始。
- 结论 (Conclusion): 1688 以图搜款必须先打开 `https://www.1688.com/` 首页，再从首页相机/以图搜款入口上传本地 Ozon 图片并选择主体图。
- 修正 (Change): 1688 supplier contract 增加 `1688_homepage_entry_required` 和 `1688_homepage_url`。
- 验证 (Verification): `python -m unittest tests.test_collection_contract_service` 通过，3 tests OK。

### 1688以图搜款滑块门禁 (1688 Image Search Slider Gate) - live-click-parse-stop
- 时间 (Time): 2026-07-06
- 操作 (Action): 从 `1688 首页 (Homepage)` 上传 Ozon 本地图片后，点击弹层里的 `搜索图片 (Search Image)`，不手动拼结果页 URL。
- 发现 (Finding): 上传成功后点击搜索触发 `滑块验证 (Slider Verification)`，页面停留在 `https://www.1688.com/`，因此无法进入 `图片搜索结果页 (Image Search Results)`。
- 证据 (Evidence): `runtime/direct-playwright/live-click-parse-stop/after-upload-before-click.png` 和 `runtime/direct-playwright/live-click-parse-stop/image-search-results.png` 均显示滑块验证弹窗。
- 结论 (Conclusion): 这次卡点是 1688 风控验证，不是 `offerId` 解析器；插件不能绕过验证，应检测到滑块后停下并等待用户人工完成。

### Playwright MCP网络路由 (Playwright MCP Network Routing)
- 时间 (Time): 2026-07-06
- 问题 (Issue): Ozon 访问必须走代理，但 1688 访问不能走代理，不能把 Playwright MCP 简单改成全局直连。
- 发现 (Finding): 当前 `@playwright/mcp` CLI 支持 `--proxy-server` 和 `--proxy-bypass`。
- 决策 (Decision): 使用同一个 `Playwright MCP (Microsoft Playwright MCP)`，默认通过 `--proxy-server` 走 Ozon 代理，同时用 `--proxy-bypass` 让 `1688/Alibaba/Alimama/图片 CDN` 域名直连。
- 边界 (Boundary): 采集网页只允许走 `Playwright MCP`；不再使用 Node Playwright runner、Codex 内置浏览器、Yandex 或旧插件采集。
