# 2026-07-04 Ozon V2 操作日志

## 固化架构契约

Layer:

- Docs
- Architecture

Files:

- `ARCHITECTURE.md`

Did:

- 建立 Ozon V2 Architecture Contract v0.1。
- 固定 FastMCP / Application / Domain / Adapter / Runtime Data 分层。
- 固定 `mcp/server.py` 只能做 FastMCP 声明和注册。
- 固定 tool 函数保持薄入口，业务逻辑进入 service。
- 固定状态机进入 `domain/state_machine.py`。
- 固定 Microsoft Playwright MCP 不嵌入 controller，只生成采集任务合约。
- 固定 Runtime Data 使用 `E:\ozon-V2工作区\OzonOpsV2`，不放业务代码。

Did not:

- 没有创建业务代码。
- 没有创建 MCP server。
- 没有创建 runtime 数据。
- 没有导入旧插件。

Verification:

- 确认 `ARCHITECTURE.md` 存在。
- 确认契约里写明不 import 旧插件、不共享旧 `D:\OzonOps`、不共享旧 `run_id`。

## 固化采集契约初版

Layer:

- Domain
- Application
- Docs

Files:

- `docs/collection_contract.md`

Did:

- 建立 Ozon V2 Collection Contract v0.1。
- 固定国内 Ozon 同行店铺识别证据：发货地、时效、Global/中国专区、店铺工商主体、履约标签。
- 固定 Ozon 爆款采集需要记录类目、属性字段、产品证据。
- 固定 1688 必须找完全一样的真实供应商产品。

Did not:

- 没有写采集器。
- 没有启动浏览器。
- 没有创建 upload 任务。
- 没有触碰旧插件。

Verification:

- 检索确认采集契约包含国内店铺识别、Ozon 产品字段、1688 exact-match、Playwright task direction、Evidence CSV direction。
