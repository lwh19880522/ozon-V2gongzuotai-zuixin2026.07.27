Regenerate only the explicitly failed slot as an exactly 3:4 portrait bitmap for the exact locked product sales unit. A square, landscape, or other ratio is invalid. The generated white subject is only an identity and geometry anchor; locked supplier evidence is final truth. Preserve exact quantity, set composition, color, silhouette, proportions, structure, parts, accessories, print, and package contents. Keep the failed slot's buyer question and its allowed `ozon-visual-v1` layout recipe, and make the scene materially different from accepted slots in at least three of environment, lighting, camera, shot scale, and buyer question.

Reuse the failed slot's original primary Ozon reference and composition blueprint unless the user explicitly changes that reference. Replace the reference product with the locked target product. Follow the reference composition, product position and scale, camera, background, lighting, negative space, and information hierarchy without copying its product identity, branding, text, watermark, price, package contents, accessories, functions, quantities, or claims.

Append the selected `slot_id`, `review_issue_code`, and `review_note` for this repair. Use them only to identify and correct the reported visual defect. The user note must not be treated as product evidence and must not authorize unsupported facts, numbers, functions, accessories, quantities, use cases, or Russian claims.

Reserve only the natural edge or bottom space required by the B edge-gradient visual system for that recipe. Keep feature structure endpoints visible for local anchor lines, and never reserve a detached rounded-card area. Do not repair typography with image generation. Do not render text, numbers, icons, logos, badges, watermarks, prices, discounts, extra products, invented parts or claims, duplicate subjects, cutout edges, collage borders, or panel labels. A plain or near-white product-only catalog treatment is invalid. Preserve exactly 3:4 portrait. Use `ozon-image-v4`, `ozon-visual-v1`, and `reference_mapping_version=ozon-reference-map-v1`.
