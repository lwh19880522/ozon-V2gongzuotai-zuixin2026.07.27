Append this single-slot wrapper after `ozon-commercial-infographic-core-prompt.txt`.

Regenerate only the explicitly failed `slot_id` as one exactly 3:4 portrait bitmap. Apply the shared core prompt to this replacement. Reference Image 1 is the same frozen verified white identity anchor used by the original attempt, the only product-identity source, and the only image input allowed. Preserve its exact silhouette, proportions, geometry, count, structure, parts, seams, openings, controls, print, material appearance, color, and selected variant. Do not reinterpret or redesign it from text. Do not attach an Ozon image, supplier evidence image, previous generated scene, conversation image, or any Reference Image 2. Reuse the failed slot's buyer question, core theme, layout archetype, and exact Russian copy. Preserve every accepted slot unchanged.

If the repaired scene cannot be made without changing Reference Image 1, simplify the scene. The repaired finished panel must be a commercial scene or infographic, never a plain or near-white product-only catalog image.

Append `review_issue_code` and `review_note` only as defect directions. They must not be treated as product evidence and must not authorize unsupported facts, numbers, functions, accessories, quantities, use cases, or Russian claims. Correct the reported product-truth, composition, scene-quality, selling-point, or other visual defect while keeping the target product exact.

Use `fixed_prompt_white_anchor` mode and reconstruct the failed slot from the fixed commercial prompt plus its frozen blueprint. Maintain a polished high-end Ozon composition without depending on any external reference image.

Render the supplied exact Russian labels as part of this first generation: the exact headline, subtitle, and 2-4 verified functional labels. Do not generate an unlabelled scene for later relabelling. Do not use a uniform edge-gradient template, tiny floating labels, a detached generic text card, or text crossing the product. Simple arrows, leader lines, measurement marks, or evidence-safe pictograms are allowed only when they point to visually proven features. Do not render extra text, URLs, prices, discounts, invented parts or claims, duplicate subjects, collage borders, or panel labels. A plain or near-white product-only catalog treatment is invalid. Use `ozon-image-v8`, `ozon-visual-v2`, `copy_mode=imagegen_integrated`, `reference_mapping_version=none`, and `guidance_mode=fixed_prompt_white_anchor`.
