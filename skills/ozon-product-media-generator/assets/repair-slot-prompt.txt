Append this single-slot wrapper after `ozon-commercial-infographic-core-prompt.txt`.

Regenerate only the explicitly failed `slot_id` as one exactly 3:4 portrait bitmap. Apply the shared core prompt to this replacement. Use the verified white identity anchor and locked 1688 evidence as product truth. Reuse the failed slot's original primary Ozon reference, buyer question, core theme, and exact Russian copy unless the user explicitly changes that reference. Preserve every accepted slot unchanged.

The verified white anchor transfers product identity and geometry only. The repaired finished panel must be a commercial scene or infographic, never a plain or near-white product-only catalog image.

Append `review_issue_code` and `review_note` only as defect directions. They must not be treated as product evidence and must not authorize unsupported facts, numbers, functions, accessories, quantities, use cases, or Russian claims. Correct the reported product-truth, composition, scene-quality, selling-point, or other visual defect while keeping the target product exact.

In `reference_guided` mode, match the mapped reference's reference-layout archetype, visual proof, subject scale and position, crop, camera, foreground/background relationship, title alignment, annotation geometry, information density, and negative-space distribution. In `ozon_aesthetic_fallback`, create a polished high-end Ozon composition without stopping. Do not reduce reference learning to a background swap. Never copy the reference product, branding, text, watermark, price, package contents, accessories, functions, quantities, or claims.

Render the supplied exact Russian labels as part of this first generation: the exact headline, subtitle, and 2—4 verified functional labels. Do not generate an unlabelled scene for later relabelling. Do not use a uniform edge-gradient template, tiny floating labels, a detached generic text card, or text crossing the product. Simple arrows, leader lines, measurement marks, or evidence-safe pictograms are allowed only when they point to visually proven features. Do not render extra text, URLs, prices, discounts, invented parts or claims, duplicate subjects, collage borders, or panel labels. A plain or near-white product-only catalog treatment is invalid. Use `ozon-image-v6`, `ozon-visual-v2`, `copy_mode=imagegen_integrated`, and `reference_mapping_version=ozon-reference-map-v1`.
