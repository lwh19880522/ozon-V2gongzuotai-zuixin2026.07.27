Append this grid wrapper after `ozon-commercial-infographic-core-prompt.txt`.

Create one horizontal 1x3 ecommerce grid with a complete canvas aspect ratio of exactly 9:4, so each panel is exactly 3:4 portrait after the equal vertical splits. Do not create a square canvas or square panels. Apply the shared core prompt independently to all three panels. No panel is a white-background anchor or a collage.

Reference Image 1 is the verified white-background identity anchor and the only product-identity source. Preserve its exact product silhouette, proportions, geometry, count, structure, parts, openings, seams, controls, print, material appearance, color, and selected variant in all three panels. Do not reinterpret or redesign it from text. If a scene cannot be made without changing Reference Image 1, simplify the scene. Every finished panel must be a commercial scene or infographic, never a plain or near-white product-only catalog image.

The complete image input list must contain exactly one item: Reference Image 1. Do not attach Reference Image 2, an Ozon image, a supplier evidence image, a previous generated image, or a conversation image. Derive all three distinct compositions only from this fixed prompt, the slot blueprints, and the supplied verified product facts. Do not blend visual directions across panels.

Panel 1 is `detail_04`: an instructional or result scene, operation, or maintenance view selected from locked facts. Do not invent a step or before/after effect. Integrate the supplied exact Russian headline, subtitle, and 2-4 verified functional labels.

Panel 2 is `detail_05`: a dimensions, fit, scale, compatibility, target-user, or second use-scene infographic. Use numeric values only when exactly verified. If no reliable metric exists, select a different evidence-safe core theme. Integrate the supplied exact Russian headline, subtitle, and 2-4 verified functional labels.

Panel 3 is `detail_06`: packaging and accessories, installation, storage, care, or another remaining buyer question that can be proved. Preserve the exact sales unit and do not create a bundle. Integrate the supplied exact Russian headline, subtitle, and 2-4 verified functional labels.

Render the supplied exact Russian labels as part of this first generation: the exact headline, subtitle, and 2-4 functional labels for each panel. Do not generate an unlabelled scene for later relabelling. Do not render any other text or numbers. Simple arrows, leader lines, measurement marks, step numbers, or evidence-safe pictograms are allowed only when the pixels and locked evidence support them. Do not render watermarks, URLs, prices, discounts, extra products, invented claims or parts, duplicate subjects, cutout edges, collage borders, or panel labels. Each pair of panels must differ in at least three of layout archetype, visible proof, environment, lighting, camera, shot scale, and buyer question. Keep equal vertical splits and preserve the exact 9:4 whole-grid ratio. Use `ozon-image-v8`, `ozon-visual-v2`, `copy_mode=imagegen_integrated`, `reference_mapping_version=none`, and `guidance_mode=fixed_prompt_white_anchor`.
