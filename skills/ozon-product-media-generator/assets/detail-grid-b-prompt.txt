Create one horizontal 1x3 ecommerce grid with a complete canvas aspect ratio of exactly 9:4, so each panel is exactly 3:4 portrait after the equal vertical splits. Do not create a square canvas or square panels. Use the exact locked product sales unit. The generated white subject is only an identity and geometry anchor; all locked supplier evidence is final product truth. Preserve exact quantity, set composition, color, silhouette, proportions, structure, parts, accessories, print, and package contents. No panel may use a plain or near-white product-only catalog treatment.

The ordered inputs for finished-scene direction are the white identity anchor, Ozon references A, B, and C. Ozon reference A maps only to Panel 1. Ozon reference B maps only to Panel 2. Ozon reference C maps only to Panel 3. Replace the reference product with the locked target product while closely following that panel's reference composition, product position and scale, camera angle, shot scale, background family, lighting, negative space, visual movement, and information hierarchy. Do not blend visual directions across panels. Do not copy reference branding, text, watermark, price, package contents, accessories, functions, quantities, or claims.

Panel 1 is `detail_04`: a lifestyle scene with a natural bottom-gradient caption zone that blends into the lower image edge and uses one verified label. It must differ from every accepted or other generated scene in at least three of environment, lighting, camera, shot scale, and buyer question. Panel 2 is `detail_05`: show scale, fit, compatibility, or small-space use only when evidence supports it, leaving a clean side edge for up to two verified labels; otherwise show a third evidence-safe context or structure, keep one label, and invent no number. Panel 3 is `detail_06`: answer the remaining buyer question through packaging, accessories, installation, structure, or a third context, leaving enough natural edge or bottom space for up to two verified labels only when both are visibly supported; without two facts, keep one. Do not reserve detached rounded-card areas. Each pair of the three panels must differ in at least three of environment, lighting, camera, shot scale, and buyer question.

Do not render text, numbers, icons, logos, badges, watermarks, prices, discounts, extra products, invented claims or parts, duplicate subjects, cutout edges, collage borders, or panel labels. Keep equal vertical splits and preserve the exact 9:4 whole-grid ratio. Use `ozon-image-v4`, `ozon-visual-v1`, and `reference_mapping_version=ozon-reference-map-v1`.
