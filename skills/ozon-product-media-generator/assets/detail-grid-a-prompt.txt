Append this grid wrapper after `ozon-commercial-infographic-core-prompt.txt`.

Create one horizontal 1x3 ecommerce grid with a complete canvas aspect ratio of exactly 9:4, so each panel is exactly 3:4 portrait after the equal vertical splits. Do not create a square canvas or square panels. Apply the shared core prompt independently to all three panels. No panel is a white-background anchor or a collage.

The verified white anchor transfers product identity and geometry only. Every finished panel must be a commercial scene or infographic, never a plain or near-white product-only catalog image.

The ordered inputs are the verified white identity anchor, locked 1688 evidence, and each panel's optional Ozon reference. The locked target product remains the only product truth. Ozon reference A maps only to Panel 1. Ozon reference B maps only to Panel 2. Ozon reference C maps only to Panel 3. Replace the reference product with the locked target product. Match each mapped reference's layout archetype, visual proof, subject scale and position, crop, camera, foreground/background relationship, title alignment, annotation geometry, information density, and negative-space distribution. Do not reduce reference learning to a background swap. When a reference is absent, use that panel's `ozon_aesthetic_fallback` blueprint and continue. Do not blend visual directions across panels. Do not copy reference branding, text, watermark, price, package contents, accessories, functions, quantities, or claims.

Panel 1 is `detail_01`: a real-use demonstration or visible result. Select one evidence-safe core theme and integrate its supplied exact Russian headline, subtitle, and 2—4 verified functional labels.

Panel 2 is `detail_02`: an annotated feature, mechanism, operation, or safety view. Use only structures visible in the locked product. Clean leader lines or close-up insets may be used only when they point to real parts. Integrate the supplied exact Russian headline, subtitle, and 2—4 verified functional labels.

Panel 3 is `detail_03`: a material, construction, detail, or reliable parameter close-up. Use a dimension or metric only when the locked evidence proves it. Otherwise select another visible, evidence-safe material or detail theme. Integrate the supplied exact Russian headline, subtitle, and 2—4 verified functional labels.

Render the supplied exact Russian labels as part of this first generation: the exact headline, subtitle, and 2—4 functional labels for each panel. Do not generate an unlabelled scene for later relabelling. Do not render any other text or numbers. Simple arrows, leader lines, measurement marks, or evidence-safe pictograms are allowed only when the pixels prove them and locked evidence supports them. Do not render watermarks, URLs, prices, discounts, extra products, invented claims or parts, duplicate subjects, cutout edges, collage borders, or panel labels. Each pair of panels must differ in at least three of layout archetype, visible proof, environment, lighting, camera, shot scale, and buyer question. Keep equal vertical splits and preserve the exact 9:4 whole-grid ratio. Use `ozon-image-v6`, `ozon-visual-v2`, `copy_mode=imagegen_integrated`, and `reference_mapping_version=ozon-reference-map-v1`.
