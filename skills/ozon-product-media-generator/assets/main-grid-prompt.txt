Append this grid wrapper after `ozon-commercial-infographic-core-prompt.txt`.

Create one horizontal 1x2 ecommerce grid with a complete canvas aspect ratio of exactly 3:2, so each panel is exactly 3:4 portrait after the equal vertical split. Do not create a square canvas or square panels. Apply the shared core prompt independently to both panels. Neither panel is a white-background anchor or a collage.

The verified white anchor transfers product identity and geometry only. Every finished panel must be a commercial scene or infographic, never a plain or near-white product-only catalog image.

The ordered inputs are the verified white identity anchor, locked 1688 evidence, and each panel's optional Ozon reference. The locked target product remains the only product truth. Ozon reference A maps only to Panel 1. Ozon reference B maps only to Panel 2. Replace the reference product with the locked target product. For each panel, match the mapped reference's layout archetype, visual proof, subject scale and position, crop, camera, foreground/background relationship, title alignment, annotation geometry, information density, and negative-space distribution. Do not reduce reference learning to a background swap. When a reference is absent, use the panel's `ozon_aesthetic_fallback` blueprint and continue. Do not blend visual directions across panels. Do not copy reference branding, text, watermark, price, package contents, accessories, functions, quantities, or claims.

Panel 1 is `main_01`: a high-impact core selling point infographic. Make the complete locked product dominant, fully visible and mobile-recognizable. Use its supplied exact Russian headline, subtitle, and 2—4 verified functional labels as an integrated part of the composition.

Panel 2 is `main_02`: a materially different real-use value overview. Make the use context or buyer value visually obvious. Use its supplied exact Russian headline, subtitle, and 2—4 verified functional labels as an integrated part of the composition. Follow reference B's information hierarchy when available.

Render the supplied exact Russian labels as part of this first generation: the exact headline, subtitle, and 2—4 functional labels for each panel. Do not generate an unlabelled scene for later relabelling. Do not render any other text or numbers. Simple evidence-safe arrows, leader lines, measurement marks, or pictograms are allowed only when the pixels and locked evidence support them. Do not render watermarks, URLs, prices, discounts, extra products, invented claims or parts, duplicate subjects, cutout edges, collage borders, or panel labels. The two panels must differ in at least three of layout archetype, visible proof, environment, lighting, camera, shot scale, and buyer question. Keep an equal vertical split and preserve the exact 3:2 whole-grid ratio. Use `ozon-image-v6`, `ozon-visual-v2`, `copy_mode=imagegen_integrated`, and `reference_mapping_version=ozon-reference-map-v1`.
