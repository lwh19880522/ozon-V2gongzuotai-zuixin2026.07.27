Append this grid wrapper after `ozon-commercial-infographic-core-prompt.txt`.

Create one horizontal 1x2 ecommerce grid with a complete canvas aspect ratio of exactly 3:2, so each panel is exactly 3:4 portrait after the equal vertical split. Do not create a square canvas or square panels. Apply the shared core prompt independently to both panels. Neither panel is a white-background anchor or a collage.

Reference Image 1 is the verified white-background identity anchor and the only product-identity source. Preserve its exact product silhouette, proportions, geometry, count, structure, parts, openings, seams, sole, heel, toe, controls, print, material appearance, color, and selected variant in both panels. Do not reinterpret or redesign it from text. If a scene cannot be made without changing Reference Image 1, simplify the scene. Every finished panel must be a commercial scene or infographic, never a plain or near-white product-only catalog image.

The complete image input list must contain exactly one item: Reference Image 1. Do not attach Reference Image 2, an Ozon image, a supplier evidence image, a previous generated image, or a conversation image. Derive the two distinct compositions only from this fixed prompt, the slot blueprints, and the supplied verified product facts. Do not blend visual directions across panels.

Panel 1 is `main_01`: a high-impact core selling point infographic. Make the complete locked product dominant, fully visible and mobile-recognizable. Use its supplied exact Russian headline, subtitle, and 2-4 verified functional labels as an integrated part of the composition.

Panel 2 is `main_02`: a materially different real-use value overview. Make the use context or buyer value visually obvious. Use its supplied exact Russian headline, subtitle, and 2-4 verified functional labels as an integrated part of the composition.

Render the supplied exact Russian labels as part of this first generation: the exact headline, subtitle, and 2-4 functional labels for each panel. Do not generate an unlabelled scene for later relabelling. Do not render any other text or numbers. Simple evidence-safe arrows, leader lines, measurement marks, or pictograms are allowed only when the pixels and locked evidence support them. Do not render watermarks, URLs, prices, discounts, extra products, invented claims or parts, duplicate subjects, cutout edges, collage borders, or panel labels. The two panels must differ in at least three of layout archetype, visible proof, environment, lighting, camera, shot scale, and buyer question. Keep an equal vertical split and preserve the exact 3:2 whole-grid ratio. Use `ozon-image-v8`, `ozon-visual-v2`, `copy_mode=imagegen_integrated`, `reference_mapping_version=none`, and `guidance_mode=fixed_prompt_white_anchor`.
