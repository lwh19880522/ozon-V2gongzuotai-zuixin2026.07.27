# 浏览器任务结果确认实施计划 (Browser Task Result Acknowledgement Plan)

1. 先补失败测试：内容脚本失败不能显示成功；无新任务时后台不能覆盖真实阶段；海外配送卡片必须进入候选。
2. 最小修改 `background.js`、`content.js` 与 `product_evidence.js`。
3. 运行浏览器脚本测试、语法检查和 Python 全量测试。
4. 仅重启 Ozon V2 工作台并一次性重载最终扩展版本。
5. 继续当前批次，逐项核对结果文件，不以心跳代替结果。
